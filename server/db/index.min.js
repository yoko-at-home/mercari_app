const{Pool:Pool}=require("pg"),connectionString=process.env.DATABASE_URL||"postgres://postgres:password@localhost:5432/mercari";console.log("here is the connectionString",connectionString);const pool=new Pool({connectionString:connectionString,ssl:{rejectUnauthorized:!1}});module.exports={pool:pool};