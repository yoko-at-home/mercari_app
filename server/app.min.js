const express=require("express"),cors=require("cors"),itemRouter=require("./routes/itemRoutes"),userRouter=require("./routes/userRoutes"),app=express();app.use(cors()),console.log("!!!"),app.use(express.json()),process.env.JWT_ACCESS_TOKEN||(console.error("環境変数が読み込まれませんでした"),process.exit(1)),app.use("/api/items",itemRouter),app.use("/api/users",userRouter),app.get("/",(req,res)=>res.send("hello world!")),module.exports=app;